export const description =
  'On this page, we’ll look at the make up of _type() method.'

# The _type() method.
The `_type()` method allows for inspecting the following object types. 

- string
- integer
- boolean
- float
- color
- table
- line
- label
- box
- linefill
- array&lt;type&gt;
- matrix&lt;type&gt;

## _type()
Get the type of an object.
<Properties>
  <Property name="this" type="object">
    Any object of type string, integer, boolean, float, color, table, line, label, box, linefill, array&lt;type&gt;, or matrix&lt;type&gt;.
  </Property>
  <Property name="returns" type="string">
    A string representation of the object type.
  </Property>
</Properties>
